{
  "name": "base-agent-wallet-bot",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "echo \"lint (optional)\"",
    "format": "echo \"format (optional)\""
  }
}