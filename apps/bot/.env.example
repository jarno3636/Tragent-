# Base mainnet RPC (Alchemy/Infura/QuickNode/etc)
RPC_URL=

# DISPOSABLE HOT WALLET PRIVATE KEY (fund with only what you can lose)
PRIVATE_KEY=0x

# Optional but recommended for higher rate limits
ZEROX_API_KEY=

# Admin token for API auth (must match config/runtime.json unless you override here)
ADMIN_TOKEN=CHANGE_ME_LONG_RANDOM

# Port Railway provides via PORT; local fallback 8787
PORT=8787

# Optional overrides
# PAUSED=true
# CONFIG_PATH=/app/config/runtime.json
